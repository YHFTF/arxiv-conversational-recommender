# Default local development dependencies: CPU-only PyTorch is installed because
# most project scripts import torch. CUDA Docker must use requirements-docker.txt
# instead; it installs the CUDA wheel and does not include this file.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torch-geometric==2.6.1
ogb==1.3.6
-r requirements-common.txt
